# Backend (`vite` proxies these paths unless you override the base URL)
VITE_API_BASE_URL=
# Google Maps JavaScript API — enable "Maps JavaScript API" and "Places API" on the same key,
# restrict it by HTTP referrer in Google Cloud Console (do not ship an unrestricted key).
VITE_GOOGLE_MAPS_API_KEY=
